/**
 * StackCardPlaceholder Component
 * Placeholder for stack card section (if needed in future)
 */

import React from 'react';
import { View, StyleSheet } from 'react-native';
import { Colors } from '../../theme/colors';

export default function StackCardPlaceholder() {
  // Placeholder component - can be expanded later if stack cards are needed
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: {
    // Empty for now, can be expanded later
  },
});

